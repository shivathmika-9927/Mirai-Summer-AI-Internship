# Echo Chamber 9000

A simple Streamlit application that collects a user's name and message, validates the input, and estimates token usage.

## Features
- Name input
- Message input
- Input validation
- Success message
- Token estimation

## Run Locally

Install Streamlit:

```bash
pip install streamlit
```

Run the app:

```bash
streamlit run app.py
```